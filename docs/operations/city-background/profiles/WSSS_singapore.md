# WSSS (Singapore) 城市背景 L2 画像

- 站点：**WSSS / Singapore**
- 坐标：1.3502N, 103.9940E
- 固定站点标签：**海岸低地**
- 地形 / 水体标签：填海低地 / 海岸填海
- 站点固定因子：高地方位不显著·东侧临海·主城东侧(城郊)
- 水体因子：沿海影响；城市方位：西南；城市相对位置：主城东侧(城郊)
- 历史样本覆盖：`2022-2025`（METAR/ISD）

---

## 1) 结论摘要（历史 METAR + 站点背景）

1. 赤道海岸填海机场，全年高湿与海峡海风主导，重点不是能不能热，而是云雨重置后还能不能恢复有效增温。
2. 峰值偏早，后段常见的是锁温或回落，而不是持续上冲。
3. 湿热滞留占比不低，露点背景会显著改写升温效率，不能只看气温斜率。
4. 降水重置日占比不低，午前启动迟滞和地面湿润残留要单独处理。
5. 风向切换敏感，主导风向组合为 N:29%, S:21%, NW:12%，转向常常比当前温度更早发出路径变化信号。
6. 日较差偏小，近水体、云层或高湿对升温上限约束较强。

## 2) L2 核心识别框架

- 核心定位：赤道海岸填海机场，全年高湿与海峡海风主导，重点不是能不能热，而是云雨重置后还能不能恢复有效增温。
- 高露点和湿层厚度几乎全年都在场，说明单看气温斜率远不如把露点、低云和降水重置放在前面。
- Changi 位于主城东侧海岸，温度振幅通常小于更内陆、更密集开发的城市热岛样本，不应套内陆热站模板。
- 转季和季风切换阶段的阵雨/雷暴触发很频繁，上午一旦被重置，午后上沿会被明显压平。

## 3) 站点固定背景

- `site_tag`: 海岸填海机场
- `factor_summary`: 高地方位不显著·东侧临海·主城东侧(城郊)
- `dominant_wind_regimes`: N:29%, S:21%, NW:12%
- `terrain_sector`: 高地方位不显著；`water_sector`: 东侧临海；`city_distance_km`: 16.9
- `climate_windows`: 东北季风(12-2) / 前转季(3-4) / 西南季风(5-9) / 后转季(10-11)

## 4) 历史 METAR 关键特征

- 站点历史画像：日较差偏小；湿热持续性强；午间云压制明显；低能见度偏多；降水重置频繁；风向切换敏感
- 暖日峰值时刻：中位 `14:00`；P75 `15:00`；晚峰占比 `8.2%`；17点后峰值占比 `1.2%`
- 日较差：中位 `6.0°C`；P90 `8.0°C`；早晨/午前/午后升温中位 `2.4°C` / `1.5°C` / `-0.2°C`
- 云与压制：晨间/午间/午后低云占比 `40.0%` / `42.1%` / `41.6%`
- 湿度与重置：热日午后露点中位 `24.2°C`；降水重置占比 `55.7%`；湿热滞留占比 `66.1%`；干混合占比 `0.0%`
- 风与能见度：轻风占比 `41.6%`；风向切换日占比 `86.2%`；低能见度占比 `32.8%`
## 5) 风向 / 日内转换图谱

- N 风：小时样本占比 `29.2%`；高发月份 Jan (5489报) / Feb (5475报)；高发时段 全天偏多。
- S 风：小时样本占比 `20.6%`；高发月份 Jul (3529报) / Jun (3041报)；高发时段 `12:00-16:00`。
- NW 风：小时样本占比 `12.3%`；高发月份 Jan (1733报) / Dec (1600报)；高发时段 `00:00-07:00`。
- 高频上午→下午转换 `NW→N`：`73` 天；高发月份 Jan (24天) / Dec (13天)；常见转换时段 `10:00`。
- 高频上午→下午转换 `SE→S`：`59` 天；高发月份 Jul (17天) / Aug (15天)；常见转换时段 `10:00`。

## 6) 云层结构 / 压制图谱

- 多层云小时样本占比 `78.2%`；高发月份 Jan (7002报) / May (6928报)；高发时段 全天偏多。
- 午间低顶多层云共存日 `764` 天；`Tmax` 中位 `31.0°C`；峰值 `13:30`；这类样本更接近“被层云/多层云持续压制”，不应只按单层低云处理。

## 7) 季节 / 气候情形分层判断

- 东北季风(12-2)：主情形 `湿热滞留(71.8%) / 风向切换(68.2%)`；主要压制因子 `降水重置`（`Tmax` 中位约压低 `1.0°C`）。
- 前转季(3-4)：主情形 `风向切换(83.8%) / 湿热滞留(60.8%)`。
- 西南季风(5-9)：主情形 `风向切换(93.9%) / 湿热滞留(67.2%)`；主要压制因子 `降水重置`（`Tmax` 中位约压低 `1.2°C`）。
- 后转季(10-11)：主情形 `风向切换(97.8%) / 降水重置(71.1%)`；主要压制因子 `降水重置`（`Tmax` 中位约压低 `1.6°C`）。

## 8) 条件化响应拆解

- 降水重置日（738 天）：午前升温从 `1.7°C` 降到 `1.1°C`。
- 低能见度日（237 天）：午前升温从 `1.5°C` 降到 `0.9°C`；峰值时刻约由 `14:00` 提前到 `13:00`。
- 风向切换日（1143 天）：午前升温从 `1.9°C` 降到 `1.4°C`。

## 9) 全年背景频率概览

- 风向切换背景：1143 天（86.2%）；高发月份 May (120天) / Jun (116天)；Tmax 中位 `31.7°C`；峰值 `14:00`；日较差 `6.0°C`；午前升温 `1.4°C`；注意：转向应被视作 live pivot，常比同小时模型误差更早提示路径变化。
- 湿热滞留背景：877 天（66.1%）；高发月份 Jan (92天) / Jun (90天)；Tmax 中位 `31.0°C`；峰值 `14:00`；日较差 `5.9°C`；午前升温 `1.2°C`；注意：露点和湿层厚度权重应抬高，单看气温斜率容易误判。
- 降水重置背景：738 天（55.7%）；高发月份 Apr (75天) / Jan (71天)；Tmax 中位 `31.0°C`；峰值 `13:30`；日较差 `6.0°C`；午前升温 `1.1°C`；注意：先防高估，除非午前明显放晴且地面升温重新建立。
- 低能见 / 静稳背景：274 天（20.7%）；高发月份 Nov (33天) / Apr (29天)；Tmax 中位 `31.0°C`；峰值 `13:00`；日较差 `6.5°C`；午前升温 `0.9°C`；注意：先看雾霾/湿层散除时点，再判断是否真正进入有效混合。
- 云压制背景：59 天（4.4%）；高发月份 Jan (14天) / Apr (7天)；Tmax 中位 `30.1°C`；峰值 `11:40`；日较差 `5.2°C`；午前升温 `-0.1°C`；注意：优先盯低云持续性和午前升温恢复，不要把暂时偏冷直接当全天失败。

## 10) 季节性窗口

- 最热月份：May (32.5°C)；Apr (32.2°C)；Oct (32.1°C)
- 午间低云最重月份：Nov (52.8%)；Dec (49.7%)；Sep (47.3%)
- 降水/重置风险更重月份：Nov (81.6%)；Dec (72.0%)；Apr (63.0%)
- 末段冲高更常见月份：May (1.6%)；Dec (1.1%)
- 湿热滞留更常见月份：Dec (86.0%)；Jun (75.0%)
- 干混合上冲更常见月份：Jan (0.0%)；Feb (0.0%)

## 11) Local Regime 图谱

- 过渡型：77 天（5.8%）；说明站点对实时路径较敏感，不宜机械套单一模板。
- 降水重置：680 天（51.3%）；Tmax 中位 `31.1°C`；峰值 `14:00`；日较差 `6.0°C`
- 湿热滞留：273 天（20.6%）；Tmax 中位 `31.6°C`；峰值 `14:00`；日较差 `5.6°C`
- 风向切换：233 天（17.6%）；Tmax 中位 `33.0°C`；峰值 `14:30`；日较差 `6.7°C`
- 云压制：59 天（4.4%）；Tmax 中位 `30.1°C`；峰值 `11:40`；日较差 `5.2°C`

## 12) 特征性天气 / 气候专题讨论

- 转季高湿无雨日分风向表现：样本 `151` 天；E 风 `21` 天 -> `Tmax` `33.0°C` / `Tmin` `27.0°C` / 日较差 `6.1°C` / 峰值 `14:01`；S 风 `24` 天 -> `Tmax` `32.5°C` / `Tmin` `26.0°C` / 日较差 `6.2°C` / 峰值 `14:00`；注意：用于区分转季阶段不同海风/季风象限下的升温效率，重点不是绝对高温，而是雨后恢复和峰值窗是否被压平。

## 13) 旧版易误判场景

1. 把 WSSS 按普通内陆热站外推，会系统性高估 clean-ramp 概率和日较差。
2. 忽略对流重置和高湿锁温，只按当前升温线性延伸，容易把上限看得过热。
3. 若忽略峰值窗低云持续性，只按当前温度线性外推，容易把云压制误当成模式冷偏差。
4. 若不单独处理雨后重置日，暖季上限会被系统性高估。
5. 晨间低能见/湿层较重时，启动偏慢更常见，不宜直接套 clean-ramp 日模板。

## 14) 实况优先观察顺序

1. 先看是否已有对流或雷阵雨重置，再看云底和露点，最后判断海风背景下午后是否还保有小幅爬升空间。
2. 露点和湿层厚度的解释优先级要提高，单看温度斜率容易误判。
3. 风向切换应视为 live pivot，必要时比同小时模式偏差更值得信任。
4. 有降水或地面湿润残留时，优先防高估；除非午前快速放晴且斜率恢复。

## 15) 仓库既有特殊规则 / 研究备注

- 当前未配置稳定可用实测探空站；Singapore 近邻高空站在现有 UWYO 文本接口不可稳定提取，运行时按 no_designated_station 处理。

## 16) 备注

- 本文由 archive 导出的历史 METAR 特征与站点固定背景联合生成，定位为更细的 L2 城市画像。
- 用途：补足旧版零碎单点城市经验，形成统一、可回放、可更新的城市背景参考层。
- 云量部分已开始显式利用原始 METAR / ISD 的多层云字段（如 `GA1/GA2/GA3/CIG`），避免把多层云结构过度简化成单一低云标签。
- 这仍是局地 METAR 行为画像，不替代后续 ERA5 环流背景判断。
