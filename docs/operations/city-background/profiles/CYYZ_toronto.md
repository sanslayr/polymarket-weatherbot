# CYYZ (Toronto) 城市背景 L2 画像

- 站点：**CYYZ / Toronto**
- 坐标：43.6777N, 79.6248W
- 固定站点标签：**湖滨缓丘**
- 地形 / 水体标签：低地缓丘 / 湖滨内陆
- 站点固定因子：西北侧高地·东南侧临水·主城西侧(城郊)
- 水体因子：近水体影响；城市方位：东；城市相对位置：主城西侧(城郊)
- 历史样本覆盖：`2022-2025`（METAR/ISD）

---

## 1) 结论摘要（历史 METAR + 站点背景）

1. 湖滨缓丘城郊机场，温度更接近“湖陆过渡场”而不是多伦多主城区热岛本体。
2. 春季(3-5)偏暖风向 `SE(16.0°C) / N(12.1°C)`；偏冷风向 `W(10.0°C) / NW(8.0°C)`；温差约 `5.1°C`；峰值时刻约 `17:00` vs `16:00`。
3. 晚峰占比高，若 15L 后仍保有斜率，不宜提前按内陆早峰模板封顶。
4. 降水重置日占比不低，午前启动迟滞和地面湿润残留要单独处理。
5. 风向切换敏感，主导风向组合为 N:20%, W:20%, SE:14%，转向常常比当前温度更早发出路径变化信号。
6. 同月最热窗口通常出现在 Jul，对应月度 Tmax 中位约 27.3°C。

## 2) L2 核心识别框架

- 核心定位：湖滨缓丘城郊机场，温度更接近“湖陆过渡场”而不是多伦多主城区热岛本体。
- 南到东南风最容易把暖湿空气直接送进机场样本，北西向更常对应冷平流或后侧干冷背景。
- 冬半年低云和能见度限制偏重，午前能否抬云开窗比单报温度更有解释力。
- 晚峰比例高，若午后仍在升温，不应按普通早峰站点提前封顶。

## 3) 站点固定背景

- `site_tag`: 湖滨缓丘
- `factor_summary`: 西北侧高地·东南侧临水·主城西侧(城郊)
- `dominant_wind_regimes`: N:20%, W:20%, SE:14%
- `terrain_sector`: 西北侧高地；`water_sector`: 东南侧临水；`city_distance_km`: 18.5
- `climate_windows`: 冬季(12-2) / 春季(3-5) / 夏季(6-8) / 秋季(9-11)

## 4) 历史 METAR 关键特征

- 站点历史画像：末段冲高倾向；午间云压制明显；低能见度偏多；降水重置频繁；风向切换敏感
- 暖日峰值时刻：中位 `16:00`；P75 `17:00`；晚峰占比 `66.7%`；17点后峰值占比 `47.1%`
- 日较差：中位 `8.0°C`；P90 `12.5°C`；早晨/午前/午后升温中位 `2.8°C` / `2.0°C` / `0.4°C`
- 云与压制：晨间/午间/午后低云占比 `23.2%` / `25.5%` / `26.3%`
- 湿度与重置：热日午后露点中位 `17.0°C`；降水重置占比 `10.6%`；湿热滞留占比 `4.3%`；干混合占比 `12.1%`
- 风与能见度：轻风占比 `25.0%`；风向切换日占比 `31.2%`；低能见度占比 `21.1%`
## 5) 风向 / 日内转换图谱

- W 风：小时样本占比 `19.1%`；高发月份 Jan (1113报) / Mar (989报)；高发时段 全天偏多。
- N 风：小时样本占比 `16.2%`；高发月份 May (986报) / Aug (811报)；高发时段 全天偏多。
- NW 风：小时样本占比 `13.5%`；高发月份 Mar (660报) / Jul (660报)；高发时段 `02:00-08:00`。
- 高频上午→下午转换 `N→SE`：`90` 天；高发月份 May (19天) / Aug (13天)；常见转换时段 `11:00-14:00`。
- 高频上午→下午转换 `SW→W`：`52` 天；高发月份 Jan (9天) / Dec (8天)；常见转换时段 `10:00`。

## 6) 云层结构 / 压制图谱

- 多层云小时样本占比 `3.7%`；高发月份 Aug (224报) / Jul (221报)；高发时段 `08:00-20:00`。

## 7) 季节 / 气候情形分层判断

- 冬季(12-2)：主情形 `午间低云压制(42.0%) / 低能见 / 静稳(32.0%)`；偏暖风向 `E/W`；偏冷风向 `SW/N`。
- 春季(3-5)：主情形 `末段冲高(45.6%) / 风向切换(30.2%)`；偏暖风向 `SE/N`；偏冷风向 `W/NW`；主要压制因子 `午间低云`（`Tmax` 中位约压低 `6.0°C`）。
- 夏季(6-8)：主情形 `末段冲高(48.8%) / 风向切换(45.4%)`；偏暖风向 `W/SE`；偏冷风向 `N/NW`；主要压制因子 `午间低云`（`Tmax` 中位约压低 `1.6°C`）。
- 秋季(9-11)：主情形 `晴空增温(28.9%) / 风向切换(27.0%)`；主要压制因子 `午间低云`（`Tmax` 中位约压低 `5.0°C`）。

## 8) 条件化响应拆解

- 春季(3-5)偏暖风向 `SE(16.0°C) / N(12.1°C)`；偏冷风向 `W(10.0°C) / NW(8.0°C)`；温差约 `5.1°C`；峰值时刻约 `17:00` vs `16:00`。
- 冬季(12-2)偏暖风向 `E(2.6°C) / W(2.0°C)`；偏冷风向 `SW(1.0°C) / N(-1.6°C)`；温差约 `2.6°C`；峰值时刻约 `17:15` vs `15:00`。
- 午间低云日（338 天）：日较差中位从 `9.0°C` 压到 `5.0°C`；午前升温从 `2.2°C` 降到 `1.0°C`；尾段斜率约 `0.8°C` vs `0.0°C`。
- 降水重置日（141 天）：日较差中位从 `8.3°C` 压到 `5.1°C`；午前升温从 `2.0°C` 降到 `1.0°C`；峰值时刻约由 `16:00` 提前到 `15:00`。
- 低能见度日（217 天）：日较差中位从 `8.4°C` 压到 `5.3°C`；午前升温从 `2.0°C` 降到 `1.0°C`。
- 末段冲高日（460 天）：峰值时刻中位约由 `15:00` 推迟到 `17:00`；尾段斜率约 `0.0°C` vs `1.5°C`。

## 9) 全年背景频率概览

- 末段冲高背景：460 天（34.7%）；高发月份 Apr (61天) / Jul (61天)；Tmax 中位 `21.0°C`；峰值 `17:00`；日较差 `9.0°C`；午前升温 `2.2°C`；注意：15L 后仍有斜率时不要过早封顶，需保留尾段上冲空间。
- 风向切换背景：414 天（31.2%）；高发月份 Jul (66天) / Aug (51天)；Tmax 中位 `21.0°C`；峰值 `16:00`；日较差 `8.3°C`；午前升温 `2.0°C`；注意：转向应被视作 live pivot，常比同小时模型误差更早提示路径变化。
- 晴空增温背景：312 天（23.5%）；高发月份 May (43天) / Jun (39天)；Tmax 中位 `23.0°C`；峰值 `16:00`；日较差 `11.0°C`；午前升温 `3.0°C`；注意：若无低云和降水残余干扰，可沿上沿路径处理。
- 低能见 / 静稳背景：265 天（20.0%）；高发月份 Jan (45天) / Dec (41天)；Tmax 中位 `6.0°C`；峰值 `16:00`；日较差 `5.1°C`；午前升温 `1.0°C`；注意：先看雾霾/湿层散除时点，再判断是否真正进入有效混合。
- 云压制背景：220 天（16.6%）；高发月份 Jan (47天) / Dec (36天)；Tmax 中位 `4.0°C`；峰值 `16:00`；日较差 `4.1°C`；午前升温 `1.0°C`；注意：优先盯低云持续性和午前升温恢复，不要把暂时偏冷直接当全天失败。

## 10) 季节性窗口

- 最热月份：Jul (27.3°C)；Aug (26.0°C)；Jun (24.0°C)
- 午间低云最重月份：Jan (49.2%)；Dec (48.9%)；Mar (35.0%)
- 降水/重置风险更重月份：Jan (36.3%)；Dec (33.0%)；Mar (30.8%)
- 末段冲高更常见月份：Apr (50.8%)；Jul (49.2%)
- 湿热滞留更常见月份：Aug (14.5%)；Jul (12.9%)
- 干混合上冲更常见月份：May (26.6%)；Jun (24.2%)

## 11) Local Regime 图谱

- 过渡型：272 天（20.5%）；说明站点对实时路径较敏感，不宜机械套单一模板。
- 末段冲高：386 天（29.1%）；Tmax 中位 `22.7°C`；峰值 `17:00`；日较差 `9.6°C`
- 云压制：220 天（16.6%）；Tmax 中位 `4.0°C`；峰值 `16:00`；日较差 `4.1°C`
- 风向切换：149 天（11.2%）；Tmax 中位 `21.3°C`；峰值 `14:00`；日较差 `9.0°C`
- 晴空增温：72 天（5.4%）；Tmax 中位 `19.1°C`；峰值 `15:00`；日较差 `9.2°C`

## 12) 特征性天气 / 气候专题讨论

- 暖季暖通道分风向表现：样本 `274` 天；S 风 `26` 天 -> `Tmax` `29.1°C` / `Tmin` `19.4°C` / 日较差 `10.0°C` / 峰值 `17:00`；SE 风 `53` 天 -> `Tmax` `27.0°C` / `Tmin` `16.0°C` / 日较差 `10.0°C` / 峰值 `17:00`；W 风 `47` 天 -> `Tmax` `28.0°C` / `Tmin` `16.0°C` / 日较差 `11.0°C` / 峰值 `16:00`；NW 风 `31` 天 -> `Tmax` `25.0°C` / `Tmin` `15.8°C` / 日较差 `10.0°C` / 峰值 `17:00`；注意：用来区分湖风/冷平流背景与真正暖湿通道，不要把 Toronto 热岛经验直接套到 CYYZ。

## 13) 旧版易误判场景

1. 把 CYYZ 当作 downtown Toronto 热岛代理，会系统性高估暖季上限。
2. 忽略湖两侧代表性差异，拿 Buffalo 一类剖面替代，会把近地层结构看偏。
3. 若在午后前段就提前封顶，最容易低估真正的晚峰或尾段冲高。
4. 若不单独处理雨后重置日，暖季上限会被系统性高估。
5. 晨间低能见/湿层较重时，启动偏慢更常见，不宜直接套 clean-ramp 日模板。

## 14) 实况优先观察顺序

1. 先看地面风是否落在 S/SE 暖湿通道，再看午前低云能否抬升，最后看雨后地面恢复速度。
2. 若 15L 后仍保有升温斜率，应保留尾段再冲一档的空间。
3. 接近窗口后段仍有斜率时，应继续保留上冲空间，避免过早封顶。
4. 风向切换应视为 live pivot，必要时比同小时模式偏差更值得信任。
5. 有降水或地面湿润残留时，优先防高估；除非午前快速放晴且斜率恢复。

## 15) 仓库既有特殊规则 / 研究备注

- 探空硬规则：Toronto 禁用 Buffalo 剖面，湖两侧代表性不一致。

## 16) 备注

- 本文由 archive 导出的历史 METAR 特征与站点固定背景联合生成，定位为更细的 L2 城市画像。
- 用途：补足旧版零碎单点城市经验，形成统一、可回放、可更新的城市背景参考层。
- 云量部分已开始显式利用原始 METAR / ISD 的多层云字段（如 `GA1/GA2/GA3/CIG`），避免把多层云结构过度简化成单一低云标签。
- 这仍是局地 METAR 行为画像，不替代后续 ERA5 环流背景判断。
