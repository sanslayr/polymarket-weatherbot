# 2026-03-01

## /look forecast pipeline iteration log

- Improved report wording and METAR presentation:
  - hold lines no longer show redundant `0.0`
  - cloud comparison compacted
  - observed max line simplified
  - 3D wording less template-like
- Added 429/breaker user alert in header.
- Changed anchor policy to full-day by default (`FORECAST_ANCHOR_LIMIT=0`).
- Added detection features:
  - frontogenesis_zone
  - llj_shear_zone
  - dry_intrusion_700
  - baroclinic_coupling
- Added 700/925-informed `object_3d` confidence + evidence support/conflict.
- Added sounding thermo hook fields (CAPE/CIN/LCL/LFC/EL pass-through ready).
- Added forecast stage timing in report header:
  - fetch / metar / forecast total
  - forecast split (cache/synoptic/decision/write)
- Added data-acquisition resilience in forecast pipeline:
  - `FORECAST_MAX_WORKERS` (default 1)
  - optional jitter `FORECAST_ANCHOR_JITTER_MS`
  - cross-runtime synoptic fallback cache (`source_state=fallback-cache`).
- Added real-time trigger pipeline module (`scripts/realtime_pipeline.py`):
  - fused-window gating (far / near_window / in_window / post)
  - window-gated P1 trigger selection for concise report output.
- P2/P3 framework review pass completed for LTAC/EGLC and archived:
  - `P2_P3_REVIEW_2026-03-01.md`
- Forecast architecture hardening updates:
  - provider-aware forecast cache key (`synoptic_provider` included)
  - provider-aware 3D bundle fallback selection
  - object_3d main-system ranking switched to weighted rank score (not distance-only)
  - baroclinic coupling score re-scaled to interpretable [0,1] range.

## Notes
- Default report style remains concise; backend is expanded but output should stay summary-first.
- Pending: local full sounding CAPE/CIN solver and P1 real-time trigger engine.

## 2026-03-01 07:56 UTC — Durable Decisions / Defaults

- Default data-source split is now fixed:
  - hourly forecast baseline: `open-meteo`
  - 3D synoptic grid: `gfs-grib2`
- GFS chain now supports runtime-cycle fallback on NOMADS 404 (e.g., t06z unavailable -> auto fallback to prior cycle with adjusted fh).
- Forecast cache behavior hardened:
  - decision schema bumped to `forecast-decision.v3`
  - coverage metrics added (`synoptic_anchors_total/ok/coverage`)
  - low coverage marks degraded and blocks forced dominant-system claims.
- Dominant-system selection policy tightened:
  - low-confidence main object is not forced as dominant;
  - promote medium/high candidate when available;
  - otherwise report “no stable dominant layered system”.
- Report style defaults updated per operator preference:
  - concise output, avoid stacking technical detail;
  - station header format: `ICAO (City) | lat, lon (site_tag)`
  - no request-received line, no routine timing breakdown noise;
  - no bias-trajectory line in METAR block.
- Site metadata is fixed in `station_links.csv` (not runtime-generated for display):
  - `site_tag` as concise station portrait term;
  - directional factors available (`water_sector`, `urban_position`) and shown succinctly.
- Synoptic wording made plain-language across system types (not front-only):
  - includes human-readable system-property line;
  - impact line gives directional implication + trigger focus.
- Tmax range output policy changed:
  - output majority-probability interval only (not full-tail envelope);
  - no “center ±x” display;
  - quantization-aware smoothing kept to avoid overfitting integer METAR jumps.
- Analysis principle agreed with user:
  - avoid rigid fixed evidence rules (not hard 2/3);
  - use dynamic evidence thresholds by phase/quality/context;
  - avoid double-counting factors already priced in by model; use trigger-over-baseline logic.
